#!/usr/bin/env ruby

require_relative "../lib/pascals_triangle"

pas_tri = PascalsTriangle.new
calculated_triangle = pas_tri.generate_triangle(ARGV[0])
line_width = calculated_triangle.last.to_s.length
calculated_triangle.each do |row|
    puts (row.to_s.center(line_width))
end
